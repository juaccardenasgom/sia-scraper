{
  "name": "sia-scraper",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js --ignore *.json",
    "cluster": "nodemon cluster.js --ignore *.json",
    "teste": "nodemon --help options",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^5.2.1",
    "puppeteer-cluster": "^0.22.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
